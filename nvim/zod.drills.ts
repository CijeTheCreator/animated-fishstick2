//Create Zod Schema
//The Schema should be an object containing string, number, boolean, date, symbol fields

//Create another Zod Schema
//This Schema should be an object containing undefined, null, void fields

//Perform a safeparse
//Store the results in a result2 variable

//Perform a safeparse with condition checking
//Store the results in a result variable
//Toast the error
//Print the data


//Infer the user type
//Store it in a User 1 variable

//Infer and Export the user type
//Store it in a User 2 variable
